# Third-party source trees fetched by porting scripts.  Never committed.
build/third_party/

# mbedTLS intermediate objects — regenerated by scripts/port-mbedtls.sh.
build/mbedtls_objs/

# mbedTLS library output.  Regenerated by the port script; don't commit
# ~700 KB of binary to git.
userland/libs/mbedtls/libmbedtls.a
userland/libs/mbedtls/include/
