# DRY consolidation phase -- sweep-1 plan (2026-07-01)

Source: 14-angle multi-agent DRY sweep (wf_0c566906-d28), each candidate
behavior-preservation + worthwhileness verified by 2 adversarial lenses.
27 confirmed consolidation units + drift-flags. Execute ONE unit/turn,
provably-safe-first, build+boot each. "drift-bug" flags are mostly the
verify lens re-stating candidates -- VERIFY each claimed divergence before
treating it as a bug.

## Shipped
- F172  console tcsetpgrp/TIOCSPGRP signal-hijack (real bug) -> one tty_set_fg_pgrp SSOT.
- F173  preempt_enable_no_resched() SSOT (10 sites).

## Confirmed units (ranked value/risk), pending
HIGH value, LOW risk:
- U-checksum [F178 DONE] transport_checksum() was __attribute__((unused)) while its body was copy-pasted 5x (udp recv+send, tcp send-seg+send-rst+recv-validate). Verified 4/5 byte-identical (same inet_pseudo_partial + word-sum + odd-byte fold + ~sum); tcp_send_rst OMITTED the `if(l) sum+=*(u8*)pw` odd-byte fold -- but RST is header-only (TCP_HDR_MIN_LEN=20, EVEN), so `l` is always 0 there and the omission never fired (latent-harmless). Made transport_checksum extern (dropped unused+static), declared in ipv4.h, routed all 5; the rst fix is by-construction (adds the dead-for-RST fold for consistency). NET gate: DHCP + tcp selftests PASS.
- U-virtio-cap [F177 DONE] find_virtio_cap + vcap_t + virtio_pci_common_cfg_t + the generic transport macros (VIRTIO_VENDOR, VIRTIO_PCI_CAP_* 1-5, VIRTIO_STATUS_* x5, VIRTIO_F_VERSION_1) were triplicated across virtio_net/gpu/input -- verified byte-identical (find_virtio_cap same logic, common_cfg field-identical). Extracted to new kernel/drivers/pci/virtio_pci.h (header-only, find_virtio_cap is static inline -> no new .c/build change); removed all 3 local copies. Device-specific constants (VIRTIO_DEV_*, per-device feature bits, GPU cmd opcodes, VTI_CFG_*, DRIVER_FEATURES) stay per-driver. Boot-critical: virtio-net DHCP + virtio-gpu init + virtio-input all exercised at boot = the gate.
- U-vaddr [F175 DONE] VERIFIED no bug: USER_ADDR_MAX (0x7FFF..FF, 2^47-1) and USER_ADDR_CEIL ((1<<47), 2^47) are NOT conflicting -- MAX is always paired with >/<= (inclusive last-valid), CEIL/(1<<47)/0x8000..00 always with >=/< (exclusive first-invalid); every site accepts exactly [0, 2^47). Pure dedup: moved the matched pair to common.h (invariant CEIL == MAX+1) as SSOT, removed the 2 local #defines (syscall.c/signal.c), routed the raw (1ULL<<47) x2 + 0x800000000000 x2 + 1 selftest vector through them. Kept BOTH constants (collapsing to one value would be wrong). Left vmm.c:755 0x8000000000000000 (2^63 top-bit kernel check, different concept). Values/operators unchanged.
- U-access-ok [F176 DONE] VERIFIED sig_user_range_ok is EXACTLY _access_ok (not weaker): both reject addr==0; addr>MAX == addr>=CEIL (CEIL==MAX+1); end-1>MAX == addr+len>CEIL; !ckd_add_u64 == (addr+len<addr) unsigned wrap; len==0 handled identically -- all input classes accept/reject the same set. No security drift. Moved _access_ok to a NEW kernel/include/uaccess.h (checked.h explicitly forbids subsystem/copy primitives), removed the syscall.c local def + fwd-decl, deleted sig_user_range_ok, routed signal.c's caller through _access_ok. uaccess.h is now the home for U-uaccess-hdr (copy_*_user decls) too. Behavior-identical; [access_ok_test]/[user_ptr_test] are the gate.
- U-ahci-const [F174 DONE] AHCI 248 PRDT / 130 page-cap literals -> AHCI_PRDT_ENTRIES/AHCI_MAX_IO_PAGES #defines (struct dim, build_prdt bound, 4 stack-array dims, xfer caps, selftest canary). Token-identical (248u/130u); build+boot verified.
- U-fifo [F179 DONE] intrusive singly-linked FIFO (enqueue-tail/dequeue-head, ->next field + count) hand-rolled 4x: udp_rx (socket.c), unix-dgram + unix-accept-backlog (unix_sock.c), io_uring CQ-overflow (io_uring.c). Verified all 4 byte-identical discipline incl the critical `if(!head) tail=NULL` empty-reset (a stale tail = UAF on next enqueue) and count++/--. Extracted FIFO_ENQUEUE_TAIL/FIFO_DEQUEUE_HEAD macros to new kernel/include/ilist.h (macro over offset-based void** to avoid strict-aliasing UB of casting type**; BSD sys/queue.h precedent). Routed all 9 sites (4 enqueue + 5 dequeue -- udp has 2). io_uring dequeue placed at the existing unlink point (n already == head there, macro re-read is a no-op) so NO reorder. Gate: unix + io_uring + pipe selftests + clean boot.
- U-strlcpy [F180 DONE] created kernel/include/kstr.h (str_eq, str_len, str_lcpy). Merged the identical: str_eq/s_streq x3 (proc.c/shmem.c/virtfs.c), str_len x3 (proc.c/ext2.c/unveil.c s_strlen), truncating copy x3 (unveil.c s_strncpy + syscall.c strncpy_k + evdev.c inline name copy). str_lcpy hardened with size==0 + NULL-src guards (both latent in the originals) and evdev's NULL-tolerance folded in. SKIPPED (DRY not a religion, noted): drm.c 3 name copies (one relies on static pre-zero for its NUL, another zero-pads all 32B as a userspace no-leak guard str_lcpy doesn't provide); str_to_uint (proc.c has 1 real def; the 2nd site validates+advances p, not a clean dup); unveil s_strncmp + inline compares in dev_find_node/ext2 (bounded-cmp / different shape). Gate: exec/path/name selftests + DHCP + clean boot.
MED/LOW:
- U-uaccess-hdr [F181 DONE] copy_from_user/copy_to_user/user_buf_check were re-declared by hand (~13 extern lines) across evdev/pty/unix_sock/pipe/io_uring/drm/futex + syscall.c's own 4 internal fwd-decls. Verified ALL type-identical to the syscall.c definitions (only param names differed -- cosmetic, no ABI drift). Added the 3 prototypes to kernel/include/uaccess.h (the F176 header, already tagged as the copy-family home), removed every scattered extern, added #include "uaccess.h" to the 7 files. user_buf_readable_ok stays syscall.c-local (no external decls); copy_path_from_user is static. Gate: [copyuser_test]/[copypath_test]/[strv_test]/[user_ptr_test].
- U-ttybg [F182 DONE] the "caller is a background process for this ctty" test (g_current && tty->fg_pgid && g_current->sid==tty->session && g_current->pgid!=tty->fg_pgid) was hand-rolled 4x: tty.c + pty.c, SIGTTIN(read) + SIGTTOU(write). Verified all 4 identical; SIGTTOU adds `&& (c_lflag & TOSTOP)` per POSIX (per-signal, kept at each site). Extracted `int tty_is_background(const tty_t*)` -- defined in tty.c (g_current = this_cpu()->current, from sched.h, is available there), declared in tty.h (NO g_current dep, so tty.h's other includers main.c/elf.c/etc. are unaffected -- an inline referencing g_current in the header would break them). Routed all 4: SIGTTIN = tty_is_background(tty); SIGTTOU = tty_is_background(tty) && TOSTOP. Byte-identical (same short-circuit order). Gate: tty/pty selftests + clean boot (Ctrl-Z/fg/bg not in headless tests).
- U-cred-id [F184 DONE] the 3-slot "id in {real,eff,saved}" membership was hand-rolled at 6 sites (MORE than the flagged 2): cred_seteuid + cred_setegid (cred.h), spawn_cred_allowed uid_ok+gid_ok (syscall.c), and sys_setreuid euid_ok + sys_setregid egid_ok (syscall.c 4256/4274 -- sweep missed these). All 6 verified byte-identical (order-independent OR of the 3 slots). Extracted cred_uid_is_one_of / cred_gid_is_one_of inlines to cred.h; spawn's gid_ok keeps `|| cred_in_group`, setre*'s ruid_ok/rgid_ok keep the DISTINCT 2-slot {real,eff}. Left distinct (per plan + POSIX): cred_setuid/cred_setgid 2-slot {real,saved}; setre* real-id 2-slot {real,eff}. Behavior-identical. Gate: [spawn_cred]/[ksec_setuid]/[signal_perm].
- U-msi [F186 DONE] extracted the SAFE common core: the standard PCI capability-list walk (for id 0x11 MSI-X / 0x05 MSI) was hand-rolled IDENTICALLY in ahci, nvme, virtio_net, virtio_input -> new pci_find_cap(bus,dev,fn,cap_id) static inline in pci.h (48-hop bounded so a cyclic cap list can't spin the kernel; behavior-identical to the walks). Routed 6 sites (ahci x2 for 0x11+0x05, nvme, virtio_net x2, virtio_input). DELIBERATELY LEFT per-driver (DRY not a religion, interrupt-critical): the MSI-X table BAR-map + per-vector program (0xFEE addr/data/ctrl) + enable -- they genuinely differ (nvme per-CPU steering, virtio_net per-queue, ahci/input single-vector, different masking + target-CPU), and a wrong merge = misrouted IRQ = I/O hang. Gate: ahci disk I/O (ext2 selftests) + virtio-net DHCP + virtio-gpu all use MSI-X.
- U-align [F187 DONE] pmm.c (align_up+align_down) and kheap.c (align_up) each hand-rolled the identical PAGE-fixed single-arg helper ((addr+PAGE_SIZE-1)&~(PAGE_SIZE-1) / addr&~(PAGE_SIZE-1); virt_addr_t==uintptr_t==64-bit so byte-identical). Moved to common.h as page_align_up/page_align_down static inlines (renamed from bare align_* -- a global single-arg align_up would be a footgun implying a 2-arg alignment; use PAGE_MASK). Removed the 3 local defs, renamed 15 callers (13 pmm + 2 kheap). Behavior-identical. Gate: pmm/slab/kheap selftests + clean boot (alignment bug = #PF/alloc fail).
- U-pgid-twin [F188 DONE] sys_getpgid/sys_getsid were byte-identical except the returned field (pgid vs sid) + the pid==0 self-case; NO perm-check difference (both unrestricted POSIX reads). Extracted pgid_sid_lookup(pid_arg, want_sid): pid==0 -> caller's own field; else rcu_read_lock + sched_find_pid + zombie/null->ESRCH + read the field INSIDE the rcu section (no UAF) + unlock. Both syscalls now thin wrappers (want_sid 0/1). Behavior-identical. Gate: process/pgid/sid selftests.
- U-chmod [F189 DONE] sys_chmod + sys_fchmod both stripped the setuid bit with the IDENTICAL condition+bit: `if (!cred_is_root(&g_current->cred)) mode &= ~S_ISUID_BIT`. Verified byte-identical (no weaker/stronger asymmetry). Extracted chmod_strip_priv_bits(mode) so the security-sensitive strip rule has one source of truth (future setgid/sticky stripping now lands in both paths automatically). The rest stays per-syscall (chmod's ksec-notify block, fchmod's fd stub). Behavior-identical. Gate: [ksec_setuid]/[fs_permop]/[spawn_cred].
- U-dcache [F190 DONE] the doubly-linked LRU tail-push splice (node->next=NULL; node->prev=tail; if(tail) tail->next=node else head=node; tail=node) was inlined at 2 sites (dentry_free_cb on d->parent, dcache_put on d) -- both add-at-tail of an UNLINKED node (guarded by prev==NULL&&next==NULL&&!head&&!tail), NOT move-to-tail, so identical. Extracted lru_push_tail_locked (exact mirror of the existing lru_push_head_locked), routed both. Placed the def before dentry_free_cb (site 1 precedes the old head-helper's position). NOTE: lru_push_head_locked is currently DEAD (0 callers -- the dcache only ever tail-pushes); left as-is (removing dead code is out of scope). Behavior-identical splice. Gate: [dcache_test]/[dcache_race]/[dcache_held].
- U-vfs-alloc [F191 PARTIAL] discovery: a factory ALREADY existed -- vfs_alloc_file() in vfs.c (kmalloc+memset0 + waitq=&_waitq/wait_queue_init + refcount=1) -- but was `static` (only vfs.c's 8 device-node opens used it). EXPORTED it (vfs.h decl) and routed the CLEAN embedded-waitq sites: ext2 (open_by_ino), socket (x2: sock + accept-child), unix_sock -- all behind ext2/DHCP/unix selftests + boot exec. SKIPPED with reason: the anon-fd trio (eventfd &s->read_wq, signalfd &s->wq, timerfd &t->wq) point waitq at an EXTERNAL state queue (NOT the embedded _waitq) -- a genuinely different pattern, do NOT force through the factory (DRY not a religion). REMAINING (routable, deferred as a focused follow-up to keep this fd-critical turn safe+verified): proc, shmem, fat32, pipe(dual-alloc), pty(x2 +secondary_waitq), tty(+secondary_waitq), io_uring(+rights), syscall(x2), drm(x2). Factory is now exported so the rest is mechanical. Behavior-identical (factory reproduces memset+waitq+refcount; callers keep non-default rights/ops).
- U-elf-unwind elf.c reimplements process.c task_create_unwind/task_init_common (VERIFY elf's fresh-image case; F156 was fork-only, elf builds fresh so no MMIO -- dedupe only).
- U-dirname    syscall.c dirname split x4 -> route through ext2 path_split (VERIFY the syscall hand-rolls' parent[] cap vs path length).

## Real drift-bugs to VERIFY then fix (not mere dedup)
- ext2 two path walkers (F174 verified: NOT a bug, NOT a merge). path_to_inode (raw, no perm) is only an internal re-resolver; every userspace path syscall goes through fs_lookup -> ext2_lookup_path(cred) first (checks EXEC on every dir component), and the mutating ops re-check write via ext2_dir_write_ok(parent, cred) on the same inode they modify. open uses ext2_open_ino by the already-resolved inode; ext2_open(raw) only after a perm-aware fs_lookup on the parent. Merging the two would double-check or break path_to_inode's internal callers -> justified separation (DRY is not a religion).
- tcp_send_rst checksum odd-byte omission (folded into U-checksum).
